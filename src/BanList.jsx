import React from "react";

function BanList({ attribute, onClick, names, attr, dataParentToChild }) {
  return (
    <div className="banList">
      <h2>Band List</h2>
      <h3>Select an attribute in your band list to band it</h3>
    </div>
  );
}

export default BanList;
